import ArrowDownIcon from "./arrowDown.svg?react";
import SettingsIcon from "./settings.svg?react";
import CheckIcon from "./check.svg?react";
import CrossIcon from "./cross.svg?react";
import SearchIcon from "./search.svg?react";
import GlobeIcon from "./globe.svg?react";
import LogoIcon from "./logo.svg?react";
import HouseSolidIcon from "./house-solid.svg?react";
import DoorOpenIcon from "./door-open.svg?react";
import PersonIcon from "./person.svg?react";
import HomeIcon from "./home.svg?react";
import CompassIcon from "./compass.svg?react";
import KeyIcon from "./key.svg?react";
import BookIcon from "./book.svg?react";
import UserBadgeIcon from "./userBadge.svg?react";

const ArrowDown = ({ className }) => {
  return <ArrowDownIcon className={className} />;
};

const Settings = ({ className }) => {
  return <SettingsIcon className={className} />;
};

const Check = ({ className }) => {
  return <CheckIcon className={className} />;
};

const Cross = ({ className }) => {
  return <CrossIcon className={className} />;
};

const Search = ({ className }) => {
  return <SearchIcon className={className} />;
};

const Globe = ({ className }) => {
  return <GlobeIcon className={className} />;
};

const Logo = ({ className }) => {
  return <LogoIcon className={className} />;
};

const HouseSolid = ({ className }) => {
  return <HouseSolidIcon className={className} />;
};

const DoorOpen = ({ className }) => {
  return <DoorOpenIcon className={className} />;
};

const Person = ({ className }) => {
  return <PersonIcon className={className} />;
};

const Home = ({ className }) => {
  return <HomeIcon className={className} />;
};

const Compass = ({ className }) => {
  return <CompassIcon className={className} />;
};

const Key = ({ className }) => {
  return <KeyIcon className={className} />;
};

const Book = ({ className }) => {
  return <BookIcon className={className} />;
};

const UserBadge = ({ className }) => {
  return <UserBadgeIcon className={className} />;
};

export {
  ArrowDown,
  Settings,
  Check,
  Cross,
  Search,
  Globe,
  Logo,
  HouseSolid,
  DoorOpen,
  Person,
  Home,
  Compass,
  Key,
  Book,
  UserBadge,
};
